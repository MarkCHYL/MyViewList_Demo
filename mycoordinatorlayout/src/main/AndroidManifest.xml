<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="demo.mark.com.mycoordinatorlayout">

    <application android:allowBackup="true" android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name" android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true" android:theme="@style/AppTheme">
        <activity android:name=".MarkActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.CoordinatorLayoutOne"/>
        <activity android:name=".ui.CoordinatorLayoutTwo"/>
        <activity android:name=".ui.CoordinatorLayoutThree"/>
        <activity android:name=".ui.CoordinatorLayoutFour"/>
        <activity android:name=".ui.CoordinatorLayoutFive"/>
    </application>

</manifest>