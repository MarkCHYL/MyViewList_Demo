<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!--图标样式-->
    <declare-styleable name="MyGraphStyle">
        <attr name="graphTitle" format="string"/>
        <attr name="xAxisName"  format="string"/>
        <attr name="YAxisName"  format="string"/>
        <attr name="textSise"  format="dimension|integer"/>
        <attr name="textColor" format="color|dimension"/>
    </declare-styleable>

</resources>
